name: Publish

permissions:
  contents: write
  
on:
  release:
    types: [published]

jobs:
  version:
    runs-on: ubuntu-latest
    name: Build and bump version
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Get version
        run: |
          tag=${{ github.event.release.tag_name }}
          echo "VERSION=${tag#v}" >> $GITHUB_ENV

      - name: Bump build version
        id: bump
        uses: vers-one/dotnet-project-version-updater@v1.3
        with:
          file: "Server.csproj"
          version: ${{ env.VERSION }}

      - run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR_ID}+${GITHUB_ACTOR}@users.noreply.github.com"
          git add .
          git commit -m "Bump project version to ${{ steps.bump.outputs.newVersion }}"
          git push

  release:
    needs: version
    name: Release
    strategy:
      matrix:
        kind: ['linux', 'windows', 'macOS']
        include:
          - kind: linux
            os: ubuntu-latest
            target: linux-x64
          - kind: windows
            os: windows-latest
            target: win-x64
          - kind: macOS
            os: macos-latest
            target: osx-x64
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Setup dotnet
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 7.0.115

      - name: Build
        shell: bash
        run: |
          tag=${{ github.event.release.tag_name }}
          release_name="LighthouseNotes-Web-$tag-${{ matrix.target }}"

          # Build everything
          dotnet publish Web.csproj --framework net7.0 --runtime "${{ matrix.target }}" -c Release -o "$release_name" --self-contained false 

          # Pack files
          if [ "${{ matrix.target }}" == "win-x64" ]; then
            # Pack to zip for Windows
            7z a -tzip "${release_name}.zip" "./${release_name}/*"
          else
          tar czvf "${release_name}.tar.gz" "$release_name"
          fi

          # Delete output directory
          rm -r "$release_name"

      - name: Publish
        uses: softprops/action-gh-release@v1
        with:
          files: "LighthouseNotes-Web-*"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}