@inherits PageLoadBase
@if (IsVisible)
{
    <MudProgressLinear Color="Color.Primary" Indeterminate="true" Size="Size.Medium"/>
}
else
{
    <MudContainer MaxWidth="@MaxWidth">
        @ChildContent
    </MudContainer>
}